"""Mission-side EXAMPLES that run against the sim through the PUBLIC pyhulax
API only — they demonstrate seams the mission project fills in.

These are NOT part of the simulator. They import `pyhulax` (the drop-in SDK)
and standard libs (cv2/numpy) — never `simcore`. On the real day the same
code runs unchanged against the real SDK; here it runs against the sim.
"""
